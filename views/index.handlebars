<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao JOBS AQUI</h1>
    <div class="navbar navbar-expand-lg navbar-light bg-light" style="margin-left: 80%; self-align: center ">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">Selecione Area:</li>
        <li class="nav-item"> <select class="form-select form-select-sm" name="" id="">
          <option value="">test</option>
        </select></li>
      </ul>
    </div>
</div>

<hr>

<h2>Vagas:</h2>

{{#each vagas}}
  <div class="card m-4">
    <div class="card-body">
      <h3>{{nome}}</h3>
      <p>{{area}}</p>      
      <a href="/vaga/{{_id}}"><button class="btn btn-success">Mais Detalhes</button></a>
    </div>
  </div>
{{else}}
  <h4>Não há vagas disponíveis </h4>
{{/each}}